<section class="studio lp-why" id="why">
  <header class="lp-section-head" data-reveal>
    <p class="kicker">(Why Vedha)&ensp;[Web · Dubai]</p>
    <h2 class="lp-section-head__title">Why ambitious brands choose Vedha.</h2>
  </header>

  <div class="studio__cards">
    <article class="s-card" data-reveal>
      <p class="kicker">(Senior-led)</p>
      <div class="avatars" aria-hidden="true">
        <img src="/images/photo-moraine-lake.jpg" alt="" width="96" height="96" loading="lazy" decoding="async" />
        <img src="/images/photo-waterfall.jpg" alt="" width="96" height="96" loading="lazy" decoding="async" />
        <img src="/images/photo-cliff-coast.jpg" alt="" width="96" height="96" loading="lazy" decoding="async" />
        <span>+1</span>
      </div>
      <p class="s-card__big">
        Senior-led, always.
        <small>No juniors learning on your project — built by senior engineers start to finish.</small>
      </p>
    </article>

    <article class="s-card s-card--dark" data-reveal>
      <p class="kicker">(Speed)</p>
      <p class="s-card__stat">Delivery time <b>−40%</b></p>
      <p class="s-card__stat">Handoffs <b>−2 weeks</b></p>
      <div class="s-card__bars" aria-hidden="true"><i></i><i></i><i></i><i></i><i></i></div>
      <p class="lp-why__note">Streamlined delivery — faster to launch, fewer weeks lost.</p>
    </article>

    <article class="s-card s-card--photo" data-reveal>
      <picture>
        <img
          class="s-card__bg"
          src="/images/photo-starry-peaks.jpg"
          alt=""
          width="1600"
          height="1032"
          loading="lazy"
          decoding="async"
          aria-hidden="true"
        />
      </picture>
      <p class="kicker">(Pricing)</p>
      <p class="s-card__big">
        Transparent pricing.
        <small>Clear, honest quotes. You pay for what your business actually needs.</small>
      </p>
      <a class="btn btn--light" href="#book" data-book-consultation>
        <span>Get a quote</span>
      </a>
    </article>

    <article class="s-card s-card--chat" data-reveal>
      <p class="kicker">(Boutique)</p>
      <p class="chat__time">Today 17:01</p>
      <p class="chat__bubble">Can we keep senior leads on this?</p>
      <p class="chat__bubble">We only take a few builds at a time</p>
      <p class="chat__time">Today 17:02</p>
      <p class="chat__bubble chat__bubble--me">Locked — your team stays senior end to end</p>
      <p class="chat__bubble">That’s why we chose Vedha.</p>
    </article>
  </div>
</section>

<style is:global>
  .lp {
    --lp-content-max: 75rem;
  }

  /* LP-only: keep hamburger, hide top nav links */
  body:has(.lp) .site-header__nav {
    display: none !important;
  }

  .lp .lp-logos,
  .lp .lp-why,
  .lp .lp-build,
  .lp .lp-work,
  .lp .lp-faq,
  .lp .lp-cta,
  .lp .lp-others {
    max-width: calc(var(--lp-content-max) + 2 * var(--gutter)) !important;
    margin-inline: auto;
    width: 100%;
  }

  .lp .lp-process__inner,
  .lp .lp-process__steps,
  .lp .lp-testimonial > * {
    max-width: var(--lp-content-max) !important;
    margin-inline: auto;
    width: 100%;
  }

  /* Shared LP section heads — kept here so Vite always ships them with the page */
  .lp-section-head {
    display: grid !important;
    gap: 0.65rem !important;
    margin-bottom: clamp(1.6rem, 3.5vh, 2.5rem) !important;
    justify-items: start;
  }

  .lp-section-head .kicker {
    margin: 0 !important;
  }

  .lp-section-head__title {
    margin: 0 !important;
    font-size: clamp(1.85rem, 3.6vw, 2.75rem) !important;
    font-weight: 600 !important;
    letter-spacing: -0.03em !important;
    line-height: 1.12 !important;
    max-width: 18ch;
    text-wrap: balance;
  }

  .lp-section-head__title--wide {
    max-width: none !important;
    white-space: nowrap;
  }

  @media (max-width: 39.99rem) {
    .lp-section-head__title--wide {
      white-space: normal;
      max-width: 22ch !important;
    }
  }

  .lp-section-head__sub {
    margin: 0 !important;
    max-width: 38ch;
    font-size: clamp(0.95rem, 1.2vw, 1.05rem) !important;
    line-height: 1.5;
    color: var(--ink-soft);
  }

  .lp-section-head__sub--wide {
    max-width: none !important;
    white-space: nowrap;
  }

  @media (max-width: 39.99rem) {
    .lp-section-head__sub--wide {
      white-space: normal;
      max-width: 36ch !important;
    }
  }

  .lp-section-head--light .lp-section-head__title {
    color: #fff !important;
  }

  .lp-section-head--light .lp-section-head__sub {
    color: rgba(255, 255, 255, 0.58) !important;
  }

  .lp-why .studio__cards {
    margin-top: 0;
  }

  .lp-why__note {
    margin-top: auto;
    font-size: 0.82rem;
    line-height: 1.4;
    color: rgba(255, 255, 255, 0.55);
  }

  .lp-why .s-card--photo .kicker {
    color: rgba(255, 255, 255, 0.55);
  }

  .lp-why .s-card--photo .s-card__big small {
    color: rgba(255, 255, 255, 0.72);
  }

  .lp-why .s-card--photo .btn {
    margin-top: 0.35rem;
  }

  @media (max-width: 47.99rem) {
    .lp .lp-why,
    .lp .lp-build,
    .lp .lp-work,
    .lp .lp-faq,
    .lp .lp-cta,
    .lp .lp-others,
    .lp .lp-logos {
      padding-block: clamp(2.6rem, 7vh, 3.75rem) !important;
    }

    .lp-section-head {
      margin-bottom: 1.25rem !important;
      gap: 0.5rem !important;
    }

    .lp-section-head__title {
      font-size: clamp(1.55rem, 6.5vw, 1.95rem) !important;
      max-width: 16ch !important;
    }

    .lp-section-head__title--wide,
    .lp-section-head__sub--wide {
      white-space: normal !important;
      max-width: none !important;
    }

    .lp-why .studio__cards {
      gap: 0.85rem !important;
    }

    .lp-why .s-card {
      min-height: 0 !important;
      padding: 1.25rem !important;
    }
  }
</style>
